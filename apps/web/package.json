{
  "name": "@atlas/web",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "vite"
  },
  "dependencies": {
    "@atlas/core": "file:../../packages/core"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.5"
  }
}
